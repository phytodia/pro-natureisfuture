<h1>Mon stock</h1>
<p>*Stocks de produits destinés à la revente. Les instituts n'ayant pas mis à jour leur stock au moins une fois par mois n'apparraissent pas dans la liste des instituts proposant le click&collect.</p>
<% current_customer.instituts.each do |institut| %>
  <% if institut.stock_institut != nil %>
    <h3>Institut: <%= institut.name %></h3>
    <table class="stock_tableau">
      <thead>
        <th>Produit</th>
        <th>Quantité</th>
      </thead>
      <tbody>
      <% institut.stock_institut.pdt_stock_items.each do |pdt| %>
        <tr>
          <td><%= Product.find(pdt.product_id).name %></td>
          <td><%= pdt.quantity %></td>
        <tr>
      <% end %>
      <tbody>
    </table>
    <br>
    <%= link_to "Modifier le stock", edit_stock_institut_path(institut.stock_institut),class:"btn-custom"%>
  <% end %>
<% end %>
