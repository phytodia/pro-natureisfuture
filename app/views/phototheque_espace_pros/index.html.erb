<h1>Phototheque</h1>
<%= link_to "Créer un dossier", new_phototheque_espace_pro_folder_path %><br>
<%= link_to "ajouter un fichier",phototheque_add_file_path%>


<ul>
  <% @phototheque.files.each do |file| %>
    <% if file.previewable? %>
      <li>
        <%= image_tag file.preview(resize_to_limit: [300, 300]) %>
        <%= link_to "Download", rails_blob_path(file, disposition: 'attachment') %>
        <%= link_to "Supprimer", delete_file_path(file) %>
      </li>
    <% else %>
      <li>
        <%= image_tag file, width:300, height:300 %>
        <%= link_to "Download", rails_blob_path(file, disposition: 'attachment') %>

        <%= link_to "Supprimer", delete_file_path(file) %>
      </li>
    <% end %>
  <% end %>
</ul>
