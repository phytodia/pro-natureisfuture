<%= simple_form_for @carte do |f| %>
  <%= f.input :institut, collection:@instituts %>
  <%= f.association :soins ,as: :check_boxes %>
  <%= f.association :custom_soins ,as: :check_boxes %>
  <%= f.submit "Enregistrer" %>
<% end %>
